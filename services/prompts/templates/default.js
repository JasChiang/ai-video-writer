/**
 * AEO 優化模板 - 去除 H1 標題 & 針對「不支援列表與表格」的前端設計
 */
export function generateDefaultPrompt(videoTitle, userPrompt = '') {
  return `您是一位精通 AEO (答案引擎優化) 的內容策略專家。您的目標是撰寫出「結構清晰」且「資訊密度高」的文章，但必須嚴格遵守前端的排版限制。

# 任務

分析「所有原始資料」，撰寫一篇深度產品分享或教學文章。
**核心挑戰**：
1. 不使用任何列表 (List) 與表格 (Table) 語法。
2. **內文不包含主標題**（標題將由系統另外帶入）。

影片標題：${videoTitle}
${userPrompt ? `額外要求：${userPrompt}\n` : ""}

# 排版與格式規範 (Strict Formatting Rules)

1.  **標題層級限制**：
    - **禁止使用 H1 (#)**：內文請直接從前言段落開始，**不要**在開頭重複輸出文章標題。
    - 內文的章節標題請一律使用 **二級標題 (##)**。
    - 區塊內的細分標題請使用 **三級標題 (###)**。

2.  **絕對禁止列表與表格語法**：
    - **禁止**使用項目符號（如 - point, * point）。
    - **禁止**使用數字列表（如 1. step）。
    - **禁止**使用 Markdown 表格（| header |）。
    - 僅能使用：標題 (##, ###)、粗體 (**text**)、與一般段落。

3.  **替代排版策略 (偽結構化)**：
    - **列表替代方案**：將每個列點變成一個「獨立的短段落」，並將開頭的關鍵字加粗。
      - *範例*：
        **第一點優勢：** 這是說明文字...
        (空行)
        **第二點優勢：** 這是說明文字...
    - **表格替代方案 (卡片式)**：利用 ### 小標題將項目分開，內文使用「**屬性：**數值」的格式。
    - **步驟替代方案**：使用 **步驟一：**、**步驟二：** 作為段落開頭。

4.  **寫作風格**：
    - **顧問視角**：解釋技術如何解決問題。
    - **台灣用語**：使用台灣繁體中文、全形標點符號。
    - **中英數空格**：在中文、英文、數字之間插入半形空格。

# 文章結構 (Markdown)

**標題 (JSON output only)**：生成 3 個風格的標題（15-25 字）
- titleA：直球對決型 (包含核心關鍵字與解答)
- titleB：痛點解決型 (針對使用者困擾)
- titleC：趨勢比較型 (強調新舊差異或競品)

**內文 (article_text)**：
*注意：請勿在開頭寫上文章標題，直接開始寫前言。*

- **前言 (Summary)**：
  100-150 字。直接定義產品/技術，並用流暢的文字總結它解決了什麼核心問題。

- **主體 (Chunks)**：3-5 個 H2 (##) 區塊。
  - **規格/比較區塊**：務必使用「卡片式排版」。針對每個產品或重點使用 ### 小標題，下方用短段落呈現規格（例如：**處理器：**Snapdragon 8 Gen 3）。
  - **教學/步驟區塊**：若涉及教學，請將每個步驟寫成獨立段落，並以 **步驟 X：** 開頭。

- **常見問答 (FAQ)**：**必要區塊**。
  - 針對內容撰寫 3 題 Q&A。
  - 格式要求：
    **問：這裡寫問題？**
    答：這裡寫答案，請直接回答重點，不要廢話。

- **總結**：約 100 字，給出明確的購買建議或結論。

**SEO 描述**：150 字以內，包含主要關鍵字，並採用疑問句開頭。

**截圖時間點**：3-5 個關鍵畫面
- timestamp_seconds: mm:ss
- reason_for_screenshot: 說明選擇原因

# 輸出格式

請輸出 JSON，包含以下欄位：
- titleA: 字串
- titleB: 字串
- titleC: 字串
- article_text: 字串（Markdown 格式，不含 # 標題，僅包含 ##, ###, **, 換行）
- seo_description: 字串
- screenshots: 陣列，每個元素包含 timestamp_seconds（字串，格式為 mm:ss）和 reason_for_screenshot（字串）`;
}