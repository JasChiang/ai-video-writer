# =============================================================================
# AI Video Writer - 環境變數配置範例
# =============================================================================
# 請複製此檔案並重新命名為 .env.local，然後填入你的實際金鑰
# ⚠️  警告：請勿將 .env.local 上傳到 GitHub 或任何公開位置！
# =============================================================================

# -----------------------------------------------------------------------------
# 🔑 必填項目（Required）
# -----------------------------------------------------------------------------

# Gemini AI API 金鑰（用於後端 AI 內容生成）
# 取得方式：https://makersuite.google.com/app/apikey
# 格式：AIzaSy 開頭，共 39 個字元
# ⚠️  此金鑰僅應在後端使用，切勿暴露在前端程式碼中
GEMINI_API_KEY=your_gemini_api_key_here

# YouTube OAuth 2.0 用戶端 ID（用於前端登入）
# 取得方式：https://console.cloud.google.com/apis/credentials
# 格式：12 位數字-英數字串.apps.googleusercontent.com
# ✅ 此 ID 可以安全地暴露在前端（OAuth 2.0 標準做法）
YOUTUBE_CLIENT_ID=your_youtube_client_id.apps.googleusercontent.com

# -----------------------------------------------------------------------------
# 🌐 網路設定（Network Configuration）
# -----------------------------------------------------------------------------

# 後端伺服器 Port
# 開發環境：3001
# 生產環境：根據部署平台設定
PORT=3001

# 前端伺服器基址（用於前端呼叫後端）
# 開發環境：http://localhost:3001
# 生產環境：https://api.yourdomain.com
VITE_SERVER_BASE_URL=http://localhost:3001

# 前端 API 基址（用於前端呼叫後端 API）
# 開發環境：http://localhost:3001/api
# 生產環境：https://api.yourdomain.com/api
VITE_API_URL=http://localhost:3001/api

# 前端網址（用於 CORS 設定）
# 開發環境：http://localhost:3000
# 生產環境：https://yourdomain.com
# ⚠️  必須精確匹配前端網址，否則會出現 CORS 錯誤
FRONTEND_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# 📁 檔案管理（File Management）
# -----------------------------------------------------------------------------

# 暫存檔案保留天數（temp_videos 和 public/images）
# 啟動時會自動清理超過此天數的檔案
# 建議值：7-14 天
FILE_RETENTION_DAYS=7

# -----------------------------------------------------------------------------
# 📝 Notion 整合（Optional - Notion Integration）
# -----------------------------------------------------------------------------

# Notion Internal Integration Token（內部整合）
# 取得方式：https://www.notion.so/my-integrations
# 用途：直接使用 token 傳送文章到指定資料庫
# 格式：secret_ 開頭
# NOTION_API_TOKEN=secret_your_internal_integration_token_here

# Notion 預設資料庫 ID
# 取得方式：開啟資料庫頁面，URL 中的 32 字元字串
# 範例：https://notion.so/{workspace}/{database_id}?v={view_id}
# NOTION_DATABASE_ID=your_database_id_32_characters

# Notion 資料庫中的標題欄位名稱
# 預設：Name（Notion 預設的標題欄位名稱）
# 如果你的資料庫使用其他名稱（如「文章標題」），請修改此值
# NOTION_TITLE_PROPERTY=Name

# Notion OAuth 2.0 設定（Public Integration）
# 取得方式：https://www.notion.so/my-integrations（建立 Public Integration）
# 用途：讓使用者登入 Notion 並選擇資料庫
# NOTION_CLIENT_ID=your_notion_public_integration_client_id
# NOTION_CLIENT_SECRET=your_notion_public_integration_client_secret

# Notion OAuth 回調網址
# 開發環境：http://localhost:3001/api/notion/callback
# 生產環境：https://your-domain.com/api/notion/callback
# ⚠️  必須在 Notion Integration 設定中新增此 URL
# NOTION_REDIRECT_URI=http://localhost:3001/api/notion/callback

# -----------------------------------------------------------------------------
# 🚫 不再使用的環境變數（Deprecated）
# -----------------------------------------------------------------------------

# YouTube Data API 金鑰（目前未使用）
# 本專案使用 OAuth 2.0 認證，不需要此金鑰
# YOUTUBE_API_KEY=your_youtube_api_key_here

# -----------------------------------------------------------------------------
# 📋 環境變數檢查清單
# -----------------------------------------------------------------------------
# 開發環境必填：
#   [✓] GEMINI_API_KEY
#   [✓] YOUTUBE_CLIENT_ID
#   [✓] FRONTEND_URL=http://localhost:3000
#   [✓] VITE_API_URL=http://localhost:3001/api
#   [✓] VITE_SERVER_BASE_URL=http://localhost:3001
#
# 生產環境額外必填：
#   [✓] FRONTEND_URL (改為實際網址)
#   [✓] VITE_API_URL (改為實際 API 網址)
#   [✓] VITE_SERVER_BASE_URL (改為實際後端網址)
#   [✓] NOTION_REDIRECT_URI (如果使用 Notion OAuth)
#
# 資安檢查：
#   [✓] .env.local 已加入 .gitignore
#   [✓] 沒有將真實金鑰提交到 Git
#   [✓] API 金鑰有設定使用限制（Google Cloud Console）
#   [✓] 定期更換 API 金鑰（建議 3-6 個月）
# =============================================================================
