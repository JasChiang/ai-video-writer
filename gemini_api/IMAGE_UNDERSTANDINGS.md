# 🖼️ Gemini 圖像理解功能

本文件概述 Google Gemini 模型處理圖像的能力，以及 AI Video Writer 專案如何利用這些功能，特別是在處理用戶上傳的參考檔案時。

## 💡 Gemini 的多模態能力

Gemini 模型從底層設計上就是多模態的，這意味著它能夠同時理解和處理多種類型的輸入，包括文字和圖像。這項能力讓 Gemini 能夠執行多種圖像相關任務，例如：

-   **圖像描述 (Image Captioning)**：為圖像生成文字描述。
-   **圖像分類 (Classification)**：識別圖像中的內容並進行分類。
-   **視覺問答 (Visual Question Answering)**：根據圖像內容回答問題。
-   **物件偵測 (Object Detection)**：識別圖像中的特定物件並標註其位置 (Gemini 2.0+)。
-   **圖像分割 (Segmentation)**：精確地識別圖像中物件的輪廓 (Gemini 2.5+)。

## 傳遞圖像給 Gemini 模型

在 AI Video Writer 專案中，當用戶上傳圖像作為參考檔案時，主要有兩種方式將圖像數據傳遞給 Gemini 模型：

### 1. 內聯圖像數據 (Inline Image Data)

-   **適用場景**：適用於較小的圖像檔案。
-   **限制**：總請求大小（包括文字提示和所有內聯數據）限制為 20MB。
-   **運作方式**：圖像數據會被編碼（通常是 Base64）並直接包含在 API 請求的 `contents` 陣列中。

### 2. 使用 File API 上傳圖像

-   **適用場景**：推薦用於較大的圖像檔案，或需要重複使用相同圖像的場景。
-   **優勢**：
    -   **處理大檔案**：File API 專為處理大文件而設計，繞過了內聯數據的 20MB 限制。
    -   **重複使用**：圖像上傳後會獲得一個 `file_uri`，可以在多個 `generateContent` 請求中重複引用，無需每次都重新上傳數據。
-   **運作方式**：圖像檔案會先透過 Gemini Files API 上傳到 Google 的基礎設施，然後在 `generateContent` 請求中透過 `file_uri` 引用。

> **AI Video Writer 中的應用**：當用戶上傳圖片、PDF 等參考檔案時，專案會利用 Gemini Files API 進行處理，以確保即使是大檔案也能被模型理解，並作為生成文章的上下文。

## 提示詞中的圖像使用

在向 Gemini 模型發送提示詞時，您可以將圖像與文字提示結合，甚至在單一提示中包含多張圖像。這使得模型能夠理解圖像的視覺內容，並根據這些視覺資訊生成更豐富、更精確的回應。

**範例 (JavaScript/TypeScript)**：
```typescript
import { GoogleGenAI } from "@google/genai";
import * as fs from "node:fs";

const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });

async function analyzeImage(imagePath: string, promptText: string) {
  const base64Image = fs.readFileSync(imagePath, { encoding: "base64" });

  const result = await ai.models.generateContent({
    model: "gemini-2.5-flash", // 或其他支援多模態的模型
    contents: [
      {
        inlineData: {
          mimeType: "image/jpeg", // 根據實際圖像類型調整
          data: base64Image,
        },
      },
      { text: promptText },
    ],
  });
  return result.response.text();
}

// 範例使用
// const response = await analyzeImage("path/to/your/image.jpg", "請描述這張圖片的內容。");
// console.log(response);
```

## 支援的圖像格式

Gemini 模型支援多種圖像格式的 MIME 類型，包括：
-   PNG (`image/png`)
-   JPEG (`image/jpeg`)
-   WEBP (`image/webp`)
-   HEIC (`image/heic`)
-   HEIF (`image/heif`)

## 限制與最佳實踐

-   **檔案限制**：Gemini 2.5 Pro/Flash, 2.0 Flash, 1.5 Pro, 和 1.5 Flash 模型支援單次請求最多 3,600 個圖像檔案。
-   **圖像品質**：使用清晰、非模糊的圖像以獲得最佳結果。
-   **提示詞順序**：當單一圖像與文字提示結合時，建議將文字提示放在圖像部分之後，以引導模型更好地理解上下文。

---

## 📚 相關文件

-   [Gemini Files API 說明](./FILES_API.md)
-   [Google Gemini 官方圖像理解文件](https://ai.google.dev/gemini-api/docs/image-understanding)
